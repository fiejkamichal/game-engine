{
  "name": "game-engine-workshop",
  "version": "0.0.0",
  "private": true,
  "description": "Turn-based game engine — workshop on multi-agent AI generation.",
  "workspaces": [
    "apps/*",
    "packages/*",
    "packages/games/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@game-engine/web",
    "build": "npm run build --workspace=@game-engine/web",
    "start": "npm run start --workspace=@game-engine/web"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
